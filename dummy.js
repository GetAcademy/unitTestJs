let value = 5;
if (z > 10) value = 10;
else value = 20;



for (let i = 0; i < model.tomatoes.length; i++) {
    const tomato = model.tomatoes[i];
    html += `<div>${tomato.weight} ${tomato.size} </div>`;
}